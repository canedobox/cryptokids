{
  "name": "cryptokids-dapp-backend",
  "version": "0.1.0",
  "author": "canedobox",
  "license": "MIT",
  "private": true,
  "scripts": {
    "node": "hardhat node",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network",
    "data": "hardhat run scripts/testData.js --network",
    "verify": "hardhat verify --contract \"contracts/CryptoKids.sol:CryptoKids\" --network"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@openzeppelin/contracts": "^4.9.2",
    "hardhat": "^2.17.0"
  }
}
